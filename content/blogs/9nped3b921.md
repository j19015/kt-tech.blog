---
id: "9nped3b921"
title: "【Next.js】 Zustandを用いた状態管理について"
category: "qrl6l2q0sxi4"
tags: ["3linszp5x1", "ezuu9zg2rg"]
eyecatch: "https://images.microcms-assets.io/assets/555ee4c8c91a46b086ad4115bdd2aeb4/5214a9a23aa747609196b8514736f813/zustand_localstorage.jpg"
createdAt: "2024-04-10T19:56:08.264Z"
updatedAt: "2024-04-11T16:16:28.245Z"
publishedAt: "2024-04-10T19:57:22.377Z"
---

# 概要

この記事では、`Zustand` とは何で、用いると何が良いのかを自分の最近得た知識として残しておきます。

# 本文

こちらの章では、本文を記述していきます。

## Zustandとは

`Zustand` は、主に`React`で利用される小さくてシンプルな状態管理ライブラリです。
`Zustand` を利用することによって、状態管理に関連するコードの複雑さを大幅に解消できます。
また、HookベースのAPIを提供しているため、`React Hooks`に慣れている開発者にとっては直感的に使うことが可能です。
どんな感じの使用感なのかテキストベースだとわかりづらいと思います。
そこで、リロード可能なカウンターアプリを作成し、「`localStorage`と`useState`」で記述した場合と、「`Zustand`」のみで記述した場合の記述の違いを見ていきます。

## useState + localStorage
何も考えずに作ったらこうなるだろうなという感じです。
`useState`と`localStorage`を使った際のカウンターを作成する際の記述が下記のコードになります。
`useState`の初期値として関数を渡して実行結果の返り値を`count`に入れたり、`count`の変更を検知したら、`localStorage`を`set`しなおす記述等が必要になっています。


```tsx:App.jsx
import React, { useState, useEffect } from 'react';

export function App() {
  const [count, setCount] = useState(() => {
    const savedCount = localStorage.getItem('count');
    return savedCount ? parseInt(savedCount, 10) : 0;
  });

  useEffect(() => {
    localStorage.setItem('count', count.toString());
  }, [count]);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>Increment</button>
      <button onClick={() => setCount(count - 1)}>Decrement</button>
    </div>
  );
}

export default App;
```

## Zustand

使用感は`Redux`に近いものだと感じました。
わざわざ`useEffect`を書くことがない点や、状態値と更新用関数をまとめて管理できるのも運用が楽だしコード量が増えないので良いなと思いました。
`storage`の設定も容易でした。

```tsx:App.jsx
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

const useCounterStore = create(
  persist(
    (set) => ({
      count: 0,
      increment: () => set((state) => ({ count: state.count + 1 })),
      decrement: () => set((state) => ({ count: state.count - 1 })),
    }),
    { name: 'counter', getStorage: () => localStorage }
  )
);

export function App() {
  const count = useCounterStore((state) => state.count);
  const increment = useCounterStore((state) => state.increment);
  const decrement = useCounterStore((state) => state.decrement);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={increment}>Increment</button>
      <button onClick={decrement}>Decrement</button>
    </div>
  );
}

export default App;
```


# 参考文献

https://zenn.dev/h_tatsuru/articles/f7ad45263fe181

https://zustand-demo.pmnd.rs/

# おわりに

`useState`と`localStorage`を使った煩雑な状態管理を統一することができる`Zustand` をこれからは選択肢として覚えておこうと思いました。
TypeScriptだと型周りちゃんとしないとなという感じです。
